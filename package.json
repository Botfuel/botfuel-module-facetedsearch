{
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap && lerna link --force-local",
    "style": "lerna run style",
    "pretest": "yarn run bootstrap && yarn run train",
    "release": "yarn test && lerna run compile --scope botfuel-dialog && lerna publish --conventional-commits --changelog-preset=angular",
    "unit-test": "BOTFUEL_APP_TOKEN=TEST_BOT jest packages/botfuel-facetedsearch",
    "test": "jest --forceExit",
    "train": "lerna run train",
    "precommit": "lint-staged",
    "start": "cd packages/sample-ecommerce && yarn start botfuel-config.js"
  },
  "devDependencies": {
    "lerna": "^3.0.0-alpha.1"
  },
  "workspaces": [
    "packages/*"
  ]
}
